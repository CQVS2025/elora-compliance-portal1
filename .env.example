# Supabase Configuration
# Required for frontend to connect to Supabase
VITE_SUPABASE_URL=https://mtjfypwrtvzhnzgatoim.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# IMPORTANT: The ELORA_API_KEY is NOT configured here!
# It must be set as a Supabase Edge Function secret using:
# supabase secrets set ELORA_API_KEY=TYjmdhZvL9dHZIT6O2DXVkfg9LZSv7X9 --project-ref mtjfypwrtvzhnzgatoim
#
# The Elora API is ONLY called by Supabase Edge Functions, never directly by the frontend.
# Edge Functions read the API key from Deno.env.get('ELORA_API_KEY')

# Emailgun (for "Email Me Now" and scheduled reports)
# Set as Supabase secrets: supabase secrets set MAILGUN_API_KEY=key-xxx MAILGUN_DOMAIN=sandboxXXX.mailgun.org
# Optional: MAILGUN_BASE_URL=https://api.eu.mailgun.net (for EU region, default is https://api.mailgun.net)
# Note: With sandbox domain, add recipient emails in Mailgun Dashboard > Sending > Authorized Recipients

# AI Insights (Anthropic Claude) - NOT in .env for the frontend
# Set as Supabase Edge Function secret so only server-side code can use it:
#   supabase secrets set ANTHROPIC_API_KEY=sk-ant-api03-xxxxx --project-ref mtjfypwrtvzhnzgatoim
# Used by Edge Functions: analyze-vehicle-risk, generate-wash-recommendations, analyze-fleet

# Database Connection (for migrations/admin tasks - DO NOT commit)
# DATABASE_URL=postgresql://postgres:your-password@db.mtjfypwrtvzhnzgatoim.supabase.co:5432/postgres
